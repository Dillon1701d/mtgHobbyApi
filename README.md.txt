Api for MtgHobbyApp, simple python flask api using tinyDB as a database

## Setup

1. Clone the repository
2. Install dependencies: `pip install -r requirements.txt`
3. Download card database: `python app/db/download_data.py`
4. Run the API: `flask run`